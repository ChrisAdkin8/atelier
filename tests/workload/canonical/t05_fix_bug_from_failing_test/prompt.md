`pytest` is failing in this fixture. Fix the production code in `duration.py` so all tests pass.

Do **not** modify the test file. The tests are the specification. If a test seems wrong, the production code is wrong.
