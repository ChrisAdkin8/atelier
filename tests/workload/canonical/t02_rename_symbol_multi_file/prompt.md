Rename the function `compute_total` to `compute_grand_total` everywhere it appears in this codebase: the definition, every caller, every test, and the README. Update any docstrings or comments that reference the old name. Do not change behavior.

After the rename, every occurrence of `compute_total` should be gone, and `pytest` should still pass.
